# 여기였지 프로젝트 규칙

- 서비스명은 여기였지다.
- 사용자 화면 문구는 한국어를 기본으로 한다.
- 웹 프로토타입은 `apps/web-prototype`에 있다.
- Node.js 서버 시작 명령은 `npm start`다.
- 기본 로컬 주소는 `http://localhost:4100`이다.
- GitHub Pages 배포 파일과 상대 경로 구조를 깨뜨리지 않는다.
- 기존 지도, 장소 저장, 기억, 컬렉션 기능을 유지한다.
- 환경변수, 비밀번호, API 키를 저장소에 커밋하지 않는다.
- 패키지를 새로 설치하기 전 반드시 필요성을 설명한다.
- 사용자 요청 없이 commit, push, force push, 브랜치 삭제를 하지 않는다.
- 작업 완료 전 `npm test`를 실행한다.
- 프론트엔드 수정 시 모바일 폭 390px 기준을 우선 확인한다.
- 변경 파일과 테스트 결과를 최종 답변에 정확히 정리한다.
